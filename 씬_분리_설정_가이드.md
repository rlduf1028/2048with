# 씬 분리 설정 가이드

## 완료된 작업

- **메인 씬 (Main.scene)**: 이름 입력 → 싱글플레이/방만들기 버튼
- **싱글플레이 씬 (SinglePlay.scene)**: 2048 solo, 역대 최고점수/내 점수, 리셋/나가기 버튼
- **멀티플레이 씬 (MultiPlay.scene)**: 초대링크 복사, 나가기 버튼

## Cocos Creator 에디터에서 할 작업

### 1. 빌드 설정 (필수)

1. **프로젝트** → **빌드** 메뉴 열기
2. **빌드** 탭에서 **참여 씬**에 아래 3개 씬 추가:
   - `Main` (시작 씬)
   - `SinglePlay`
   - `MultiPlay`
3. **시작 씬**을 `Main`으로 설정

### 2. 메인 씬 컴포넌트 확인

Main.scene을 열었을 때 **MainSceneManager** 컴포넌트가 Canvas에 없으면:

1. **계층**에서 **Canvas** 선택
2. **속성** 패널에서 **컴포넌트 추가**
3. **MainSceneManager** 스크립트 선택

### 3. 서버 leave_room 이벤트 (선택)

멀티플레이에서 **나가기** 버튼을 누르면 `leave_room` 이벤트를 서버로 보냅니다.  
서버에서 해당 이벤트를 처리하는지 확인하세요.

## 씬 흐름

```
Main (이름 입력 → 싱글플레이/방만들기)
  ├─ 싱글플레이 클릭 → SinglePlay
  └─ 방 만들기 클릭 → 방 생성 → MultiPlay

SinglePlay (리셋/나가기)
  └─ 나가기 → Main

MultiPlay (초대링크 복사/나가기)
  └─ 나가기 → Main
```
